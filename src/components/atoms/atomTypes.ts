export type BadgeType = {
  text?: string;
  styles?: string;
  background?: string;
  textColor?: string;
};

export type TitleType = {
  text: string;
  size?: "xs" | "sm" | "base" | "lg" | "xl" | "2xl" | "3xl" | "4xl" | "5xl";
  styles?: string;
  textColor?: string;
};
